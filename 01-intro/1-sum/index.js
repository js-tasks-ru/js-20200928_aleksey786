/**
 * sum
 * @param {number} m base
 * @param {number} n index
 * @returns {number}
 */
export default function sum(m, n) {
  if (m===undefined) {
    return m;
  }
  if (n===undefined) {
    return n;
  }
  return m + n;
}
