/**
 * sum
 * @param {number} m base
 * @param {number} n index
 * @returns {number}
 */
export default function sum(m, n) {
  if (m===undefined) {
    return m;
  } else if (n===undefined) {
    return n;
  } else {
    return m + n;
  }
}
